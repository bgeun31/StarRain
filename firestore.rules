rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 개발 중에는 모두 허용 (운영 시 인증 기반으로 변경 필요)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
